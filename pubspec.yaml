name: project3
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  video_player: ^2.7.0  
  cupertino_icons: ^1.0.6
  lucide_icons: ^0.257.0
  provider: ^6.1.1
  flutter_spinkit: ^5.2.0
  camera: ^0.10.5+2
  http: ^1.1.0
  image_picker: ^1.0.4      # مطلوب لرفع الصور
  path_provider: ^2.1.1     # مطلوب للوصول إلى مسارات الملفات
  path: ^1.9.0              # مطلوب لمعالجة المسارات

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/

    